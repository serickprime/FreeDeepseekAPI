'use strict';
const cache = new Map();
async function load(url, timeoutMs) { if (!url) throw new Error('Missing PoW WASM URL'); if (!cache.has(url)) { const p = fetch(url, { signal: AbortSignal.timeout(timeoutMs) }).then(async r => { if (!r.ok) throw new Error(`PoW WASM HTTP ${r.status}`); return WebAssembly.compile(await r.arrayBuffer()); }); cache.set(url, p); p.catch(() => cache.delete(url)); } return cache.get(url); }
async function solvePOW(challenge, url, timeoutMs = 15000) { const instance = await WebAssembly.instantiate(await load(url, timeoutMs), { wbg: {} }); const e = instance.exports; const enc = new TextEncoder(); const c = enc.encode(challenge.challenge); const p = enc.encode(`${challenge.salt}_${challenge.expire_at}_`); const cp = e.__wbindgen_export_0(c.length, 1) >>> 0; const pp = e.__wbindgen_export_0(p.length, 1) >>> 0; new Uint8Array(e.memory.buffer, cp, c.length).set(c); new Uint8Array(e.memory.buffer, pp, p.length).set(p); const sp = e.__wbindgen_add_to_stack_pointer(-16); e.wasm_solve(sp, cp, c.length, pp, p.length, challenge.difficulty); const code = new DataView(e.memory.buffer).getInt32(sp, true); const answer = new DataView(e.memory.buffer).getFloat64(sp + 8, true); e.__wbindgen_add_to_stack_pointer(16); if (!code || !Number.isFinite(answer)) throw new Error('PoW failed'); return Math.floor(answer); }
module.exports = { solvePOW };
